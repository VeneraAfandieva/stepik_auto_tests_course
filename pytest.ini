[pytest]
markers =
    smoke: marker for smoke tests
    regression: marker for regression tests
    win10
    xfail_strict=true

# content of pytest.ini
addopts = 
    -ra -q


norecursedirs = 
    out build
